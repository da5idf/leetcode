var dailyTemperatures = function (temperatures) {
    const queue = [[temperatures[0], 0]];
    let days = 1;
    for (let i = 1; i < temperatures.length; i++) {
        let curr = 
    }

    return temperatures
};