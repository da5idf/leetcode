var coinChange = function (coins, amount) {
    const set = new Set();

    for (let coin of coins) {
        set.add(coin);
    }

    let possible = true;
    while (amount > 0 || possible) {

    }
};

coinChange([1, 2, 5], 11);